import { styled } from '@mui/material/styles';
import { FormControlLabel } from '@mui/material';

const CustomFilterCheckBox = styled(FormControlLabel)({
  padding: '20px',
  fontSize: '18px',
  fontWeight: 'bold',
});

export default CustomFilterCheckBox;